# Until the first configuration from the controller: no traffic ports.
global
    log stdout format raw local0

defaults
    mode http
    timeout connect 2s
    timeout client 30s
    timeout server 30s

listen bootstrap
    bind 127.0.0.1:8404
    stats enable
    stats uri /
