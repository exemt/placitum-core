# The network of this machine comes from /etc/netplan/99-placitum.yaml, not from cloud-init:
# the configuration cloud-init renders is tied to the adapter it saw, and an image carries it to
# machines with other adapters. Everything else cloud-init does — users, ssh keys, the answers
# file of the first boot — works as usual.
network: {config: disabled}
