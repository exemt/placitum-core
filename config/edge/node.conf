# Идентичность узла для модуля. Имя обязано совпадать с PLC_NODE_ID из .env:
# по нему нода видна в панели, в пульсе и в аудите. install.sh переписывает
# этот файл под значение из .env, поэтому править лучше там.
#
# Шину и всё остальное печатает поколение контроллера: второй waf_bus здесь
# сделал бы `nginx -t` ошибкой "directive is duplicate".
waf_node_id edge-01;
